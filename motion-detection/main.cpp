/**
 * opencv_template.cpp
 *
 * Template for an opencv program (using opencv 3), 
 * containing typical headers.
 */

#include <opencv2/opencv.hpp>
#include <opencv2/core/core.hpp>
#include <opencv2/imgcodecs.hpp>
#include <opencv2/highgui/highgui.hpp>

#include <iostream>
#include <string>

using namespace cv;
using namespace std;

int main(int argc, char** argv) {
  // Load in the image. Video. 
  // Split it into NxN grids 
  // Update the mean 
  // Update the variance 
  // Update the age 
  // Check apprent model and candidate model, update accordingly
  return 0;
}

